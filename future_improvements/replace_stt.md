# Replace STT from API to Local Models

Currently, Speech-to-Text (STT) uses an API. In the future, this should be replaced with local models (e.g., local Whisper) to improve latency, privacy, and reduce dependency on external services.
